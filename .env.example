DFMCP_BRIDGE_ADAPTER=file_queue
# For Dockerized backend on Windows/macOS, use host.docker.internal for local_http.
# For a host-local backend process outside Docker, use 127.0.0.1.
DFMCP_LOCAL_HTTP_HOST=host.docker.internal
DFMCP_LOCAL_HTTP_PORT=5001
